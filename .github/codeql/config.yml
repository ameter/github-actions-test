name: "CodeQL config"
packs:
  - uses: actions
queries: 
  - javascript:
    - uses: security-and-quality
    # - name: Run custom queries
    #   uses: ./.github/codeql/queries
paths:
  - .github
  - .codeql
  - fastlane