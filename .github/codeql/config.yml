name: "CodeQL config"
# packs:
#   - uses: actions
queries: 
  #- javascript:
    #- uses: security-and-quality
  - uses: github/codeql/javascript/ql/src/experimental/semmle
    # - name: Run custom queries
  - uses: ./.github/codeql/queries/experimental/semmle
paths:
  - .github
  - .codeql
  - fastlane
