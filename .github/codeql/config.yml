name: "CodeQL config"
# packs:
#   - uses: actions
queries: 
  - javascript:
    #- uses: security-and-quality
    - uses: ./.github/codeql/queries/experimental/Security
    # - name: Run custom queries
    #   uses: ./.github/codeql/queries
paths:
  - .github
  - .codeql
  - fastlane