name: "CodeQL config"
# packs:
#   - uses: actions
queries: 
  - javascript:
    #- uses: security-and-quality
    - uses: codeql/javascript/ql/src/experimental/semmle/javascript/Actions.qll
    # - name: Run custom queries
    #   uses: ./.github/codeql/queries
paths:
  - .github
  - .codeql
  - fastlane