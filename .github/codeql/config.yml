name: "CodeQL config"
# packs:
#   - uses: actions
queries: 
  - javascript:
    #- uses: security-and-quality
    - uses: codeql/javascript/ql/src/experimental/Security/CWE-094
    # - name: Run custom queries
    #   uses: ./.github/codeql/queries
paths:
  - .github
  - .codeql
  - fastlane